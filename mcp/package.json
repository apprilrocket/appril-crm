{
  "name": "@appril/mcp-campaigns",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server del CRM de Appril — permite a una IA gestionar contactos, listas, copys, campañas y secuencias sobre la BD del CRM. Nunca envía de forma autónoma: prepara borradores; el envío real lo gatilla un humano o el scheduler tras aprobación.",
  "bin": {
    "appril-mcp-campaigns": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:integration": "tsx test/integration.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.89.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
